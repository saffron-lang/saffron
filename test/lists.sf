var a = [1,2,3];
IO.println("Initial List: ", a);
IO.println("List Length : ", a.length());
IO.println("Copying List", "");
var a2 = a.copy();
a.push(4);
IO.println("After Push  : ", a);
var b: Number = a.pop();
IO.println("After Pop   : ", a);
IO.println("Pop Value   : ", b);
a.reverse();
IO.println("Reversed    : ", a);
var c: Number = a[1];
IO.println("Val at idx 1: ", c);
IO.println("Copied List : ", a2);
var d = [5, 3, 1, 4, 2, 0];
IO.println("New List    : ", d);
IO.println("Sorting List");
d.sort();
IO.println("Sorted List : ", d);