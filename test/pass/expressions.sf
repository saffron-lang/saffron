fun add_one(x: Int) {
    return x + 1
}

fun times_two(x: Int) {
    return x * 2
}

5
  |> add_one()
  |> times_two()
  |> IO.println()

var b: Int;
b = 3;
IO.println(b);

var a = fun () => {
    IO.println("Wow!");
};

IO.println(a());
IO.println(:atom == "atom");
IO.println(:atom == :atom);
