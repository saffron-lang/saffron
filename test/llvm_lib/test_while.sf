fun sum_to(n: Int): Int {
    var total: Int = 0
    var i: Int = 1
    while (i <= n) {
        total = total + i
        i = i + 1
    }
    return total
}

fun main() {
    var result: Int = sum_to(10)
    IO.println(result)
}
