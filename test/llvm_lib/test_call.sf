fun double(x: Int): Int {
    return x * 2
}

fun apply_twice(x: Int): Int {
    var first: Int = double(x)
    var second: Int = double(first)
    return second
}

fun main() {
    var result: Int = apply_twice(3)
    IO.println(result)
}
