fun add_one(x: Number) {
    return x + 1
}

fun times_two(x: Number) {
    return x * 2
}

5
  |> add_one()
  |> times_two()
  |> println()

var b: Number;
b = 3;
println(b);

var a = fun () => {
    println("Wow!");
};

println(a());
println(:atom == "atom");
println(:atom == :atom);
