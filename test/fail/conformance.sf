// Expected errors:
// - Missing implementation for 'area' from interface
// - Missing implementation for 'perimeter' from interface

interface Shape {
    fun area(): Int
    fun perimeter(): Int
}

class Circle extends Shape {
    var radius: Int
    fun init(r: Int) {
        this.radius = r
    }
    // Missing area() and perimeter() implementations
}

IO.println("should error above")
