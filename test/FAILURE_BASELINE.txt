# Current suite failure baseline — one test name per line, sorted.
#
# Why this file exists: every BUGS.md entry that reports a suite result quotes a
# baseline count inline ("the 24-name baseline", "the 21-entry baseline"), and
# those counts are correct only on the day they were written. Reading a stale one
# as current is how a regression hides — a diff against the wrong baseline shows
# a name "already failing" when it started failing with your change.
#
# Compare failure SETS by name, never totals. `passed` moves when tests are added
# or skipped, so equal totals across two runs prove nothing. Extract a set with:
#
#   tools/run_tests.sh > /tmp/suite.log 2>&1
#   awk '/^=== Failures ===/,/^TOTAL/' /tmp/suite.log \
#     | grep -E '^  [a-z-]+ +[a-zA-Z0-9_/]+' | awk '{print $2}' | sort -u \
#     > /tmp/fail_now.txt
#   comm -23 test/FAILURE_BASELINE.txt /tmp/fail_now.txt   # fixed by your change
#   comm -13 test/FAILURE_BASELINE.txt /tmp/fail_now.txt   # REGRESSIONS
#
# Update this file in the same commit that changes the set, and say in the commit
# message which names moved and why. A shrinking set with no explanation is as
# suspect as a growing one: `tools/run_tests.sh` skips network tests and known-
# stale ones by default, so a test can leave this list by being skipped rather
# than by being fixed.
#
# gc_roots_test, test_reflect and test_glob were all once in the "nondeterministic,
# treat as noise" category. None of them are any more — each was genuinely fixed on
# 2026-08-03: an unguarded `ptr - 24` on an Any, a bare inttoptr in load64/store64,
# and (test_glob) BUGS #153 plus two logic errors in `@glob` itself. A failure from
# any of the three is now real signal. test_glob's apparent nondeterminism was a
# segfault from indexing a NaN-tagged String, which is allocator-dependent and so
# looked flaky; it now passes 5/5 at a fixed HEAD.
#
# Recorded 2026-08-03 at 4fb2690 (merge of phase5-internal): 262 passed, 16
# failed, 14 skipped, 1 known-fail.
#
# Updated 2026-08-03 at the ide-stage0-spans merge: 275 passed, 11 failed, 14
# skipped, 1 known-fail. That merge fixed five names, all removed below:
# enum_cross_simple, imports, inheritance (module-system / @ast import fixes and
# the alias/type-re-export fix now #149), pass/data_equality (structural ==
# reaching more shapes), and test_lib_repro. No regressions: the 11 remaining
# names are a strict subset of the previous 16.
#
# Updated 2026-08-03 at the origin/main merge (86ab8e6): 278 passed, 9 failed,
# 14 skipped, 1 known-fail. origin/main's `\xNN` escape-decode fix (#152, was
# origin's #146 before the collision renumber) fixed test_base64 and test_log,
# both removed below. No regressions: the 9 remaining names are a strict subset
# of the previous 11.
#
# Updated 2026-08-03 at the BUGS #148 fix (nonexistent class-member read): 282
# passed, 9 failed, 14 skipped, 1 known-fail. The failure SET below is unchanged —
# nothing was fixed or broken. The count moved for two reasons that are worth
# spelling out, because each one on its own looks like a result: +3 are this fix's
# own tests (fail/class_member_missing, fail/class_member_missing_no_fields,
# pass/class_member_present), and +1 earlier was pass/cross_module_subclass
# arriving with upstream's c28ebca for #151. Exactly the case this file's header
# warns about — a rising `passed` proving nothing about the set.

# Updated 2026-08-03 at the ph5 #153 merge (unresolved-type subscript routed to a
# runtime dispatch): 281 passed, 8 failed, 14 skipped, 1 known-fail. One name
# removed, test_glob — the subscript fix plus real recursive descent in `@glob`'s
# find_in and a trailing-slash fix in _ignore_matches. No regressions: the 8
# remaining names are a strict subset of the previous 9.
#
# Confirmed 2026-08-03 after merging the #148 fix with ph5 #153 and compiler-
# rewrite stage 1: 285 passed, 8 failed, 14 skipped, 1 known-fail. The SET is
# unchanged from the 8 above — the three merged changes compose, with bootstrap
# green including the STAGE 2 gen4 fixed point. The +4 in `passed` is #148's three
# new tests plus one that arrived upstream; no name entered or left this list.
#
# Confirmed 2026-08-04 with #143's regression tests: 291 passed, 8 failed, 14
# skipped, 1 known-fail. The SET is unchanged. No compiler source changed in that
# commit at all — #143 was already fixed in 2c34dc6 and had no tests, so the +6 is
# purely the five test/fail/cond_not_bool*.sf files and test/pass/cond_bool_ok.sf.
# A `passed` count that moves while the compiler does not is the clearest possible
# case for this file's rule about comparing sets rather than totals.
#
# Confirmed 2026-08-04 after the BUGS #115 and #156 fixes: 294 passed, 8 failed,
# 14 skipped, 0 known-fail. The SET is unchanged. Two things about the counts are
# worth spelling out because each looks like a result on its own:
#
#   - `known-fail` went 1 -> 0. #115's fix made oracle_println_class_not_bits pass,
#     and a KNOWN_FAIL test that passes is reported as an `xpass` *failure*, so its
#     entry had to be dropped from tools/run_tests.sh in the same commit. The
#     KNOWN_FAIL list is now empty.
#   - `passed` 291 -> 294 is three added tests, not three repairs:
#     pass/list_lit_elem_type (#117, later strengthened), the strengthened
#     oracle_println_class_not_bits (#115), and pass/nil_init_infers_any (#156, filed as #155).
#
# oracle_enum_println stays on this list on purpose — it is the untouched enum half
# of #115's problem, now filed as #154, and its output was byte-identical before
# and after both fixes.
#
# Confirmed 2026-08-04 on the merged tree (#147 + #155 from main, #156 and #157
# from this line): 296 passed, 8 failed, 14 skipped, 0 known-fail. The SET is
# unchanged, and the +2 over the previous note are this line's two new tests
# (pass/import_alias_punctuation for #157, and pass/nil_init_infers_any counted
# once #156 landed) — no name entered or left.
#
# Worth recording that this run is a MERGE check, not just a change check. #147
# moved the "was an annotation written?" sentinel from "Any" to "" in the same
# checker VarDecl arm #156 rewrote, so the two could have composed badly while each
# passed alone. They do not: 8/8 and 14/14 respectively, plus first_class_types
# unchanged. An auto-merge that produces no conflict is not evidence the result is
# correct — the two sides' tests have to be re-run together, which is what this
# line records.
#
# Confirmed 2026-08-04 with the BUGS #159 fix (a class- or enum-typed condition is
# now a checker error): 299 passed, 8 failed, 14 skipped, 0 known-fail. The SET is
# unchanged and the +3 is this fix's own three tests (fail/cond_not_bool_class,
# fail/cond_not_bool_enum, pass/cond_class_enum_ok).
#
# This particular change is one where an unchanged SET is the *interesting* result
# rather than a formality. #159 makes the checker reject a shape it used to accept,
# so the risk is not that it fails to fire but that it fires too widely — and every
# over-firing would show up here as a name entering the list. None did, and the same
# rule ran clean over the compiler's own source in the stage 2 gen4 fixed point.
# `pass/cond_class_enum_ok.sf` is the deliberate record of the let-through set
# (interface-typed and union-typed conditions still compile), so the boundary is
# asserted in a test rather than only argued in BUGS.md.
#
# Updated 2026-08-04 rebasing the lsp-symbol-payload line (located expression
# diagnostics, WS1c) onto 9ca7def: 300 passed, 8 failed, 14 skipped, 0 known-fail.
# The SET below is UNCHANGED — comm-diffed both directions, empty each way, no
# name fixed and none regressed. The +1 over #159's 299 is this line's own
# test/pass/located_checker_diagnostics.sf, confirmed PASS by name in the log.
#
# By now this file's own history has recorded SIX different `passed` totals
# against the very same 8-name set — 286, 291, 295, 296, 299 and 300 — and not one
# of the moves was a repair. Every one was tests arriving: #143's, #115/#117's,
# the base's pass/explicit_any_annotation, #156/#157's pair, #159's three, and now
# this line's one. Six totals, one set.
#
# The merge-check point two notes up applies to this rebase too, and this is the
# case it was written for. #159 added two `hint` branches to the very same
# `check_condition` line this line converted from `error` to `error_at` — so the
# two changes were not merely in the same file, they were on the same statement.
# Git flagged it, both sides were kept (the enum/class hints AND the span), and the
# result was then verified three ways rather than assumed:
#
#   - fail/cond_not_bool_class and fail/cond_not_bool_enum are still REJECTED, so
#     #159's rule still fires. Keeping a diagnostic's text while silently losing
#     its trigger is the failure mode a green `passed` count would have hidden.
#   - `--json` on a class-typed condition returns located:true with line 5,
#     column 9 — the condition identifier itself, not the enclosing statement — so
#     the span half works on the merged line, not just the message half.
#   - the message carries #159's hint verbatim, so neither side was flattened into
#     the other.
#
# A conflict here was the good outcome. Had git auto-merged these two edits, one
# side would have been lost silently and every count in this file would still have
# looked exactly like this.
#
# Confirmed 2026-08-04 with the BUGS #163 fix (three import-line defects): 308
# passed, 8 failed, 14 skipped, 0 known-fail. The SET below is UNCHANGED --
# comm-diffed both directions, empty each way. The +3 over the merged tree's 305
# is this fix's own three tests, each traced BY NAME in the run's log:
# fail/import_missing_from and fail/import_unquoted_path (both `rejected, exit 1`)
# and pass/import_forms_ok. That is a SEVENTH total against the same 8-name set.
#
# One methodology note, because it cost a wrong answer once in this session: do
# not `comm` this file directly. It is mostly comments, and `comm -23` happily
# reported all ~130 comment lines as "fixed by your change" -- a diff that looks
# alarming and means nothing. Strip them first:
#
#   grep -vE '^\s*#|^\s*$' test/FAILURE_BASELINE.txt | sort -u > /tmp/base.txt
#
# The extraction recipe at the top of this file produces names only, so only the
# baseline side needs stripping. The header says to compare sets by name; this is
# the mechanical corollary nobody had written down.
#
# Why an unchanged set is the load-bearing result here: two of the three fixes add
# parse errors, so the risk is over-firing, and every import line in the tree
# (411 `import "path"`, 7 `import { names } from "path"`) goes through the changed
# code. A single over-fire would have taken out most of the suite at once. It is
# also why bootstrap green proves little on its own -- the compiler's own source
# contains no malformed import, so all three defects were invisible to the gen4
# fixed point while fully present.
#
# Updated 2026-08-04 at the #168/#167 work: 302 passed, 9 failed, 14 skipped.
# ONE NAME ADDED, pass/check_module_imports, and it is added under protest — the
# test is right, the compiler is wrong, and the bug is BUGS #162 (an object held
# only in an LLVM SSA temp is never rooted, so a collection frees it) rather than
# anything in #168's duplicate-declaration pass.
#
# The evidence, because "my change is innocent" is exactly the claim that needs
# it. check_module_imports runs the checker IN-PROCESS, so it executes the new
# pass. Neutralising the pass to `if (stmts.length() >= 0) { return }` placed
# AFTER its `var seen: Map<String, String> = {}` still segfaults; moving the same
# early return BEFORE the Map allocation passes. One extra Map allocation per
# module checked is the entire difference. `GC.disable()` also makes the test pass
# 4/4. So the pass's logic is not reachable by the crash — only its allocation is,
# and what it perturbs is allocation timing.
#
# #167 fixed the runtime half of that family (six container constructors that
# allocated child buffers while the parent was unrooted) and this test is not
# fixed by it, which is the useful negative result: two independent layers, and
# only one is closed. #162's own repro still yields 51 corrupted reads out of 200
# after #167, unchanged. It is owned by the saffron_154 line, so it is recorded
# here rather than fixed here.
#
# Removing this name again is the acceptance test for #162. If someone lands an
# SSA-temp rooting fix in codegen, this is the test that should start passing
# without being touched.
#
# Confirmed 2026-08-04 on the MERGED tree (#168/#167 above, merged with origin's
# #155-scalar, #158, #160, #163, and the FunDecl/Lambda ret_type + assignment type
# checks): 311 passed, 9 failed, 14 skipped. The SET is exactly the 9 names below —
# comm-diffed both directions against this file, empty each way — and md5 of
# build/saffronc was 174ea698f90c791749ecd89d6dfff53d before and after the run.
#
# The count arithmetic closes exactly, which is the part worth writing down because
# a merge is where it usually stops closing: origin's 308 passed / 8 failed, PLUS
# this line's four new tests all passing (pass/gc_constructor_rule,
# pass/duplicate_decl_ok, fail/duplicate_class, fail/duplicate_enum_class), MINUS
# pass/check_module_imports moving into the failure column. 308 + 4 - 1 = 311, and
# 8 + 1 = 9. No name is unaccounted for in either direction.
#
# This was the run the merge actually needed, not a formality. origin migrated
# FunDecl/Lambda `ret_type` from String to AST.Type in the same checker.sf that
# #168 adds a pass to, and added an assignment type check there too. A clean
# auto-merge of that pair says nothing — the presence of check_duplicate_decls in
# the merged file was verified by grep at merge time, but only the STAGE 2 gen4
# fixed point (0 unresolved inference fallbacks, gen3 compiles itself) proves the
# merged source still type checks against itself, and only this diff proves the
# two sides' behaviour composes.

# Confirmed 2026-08-04 with the payload half of BUGS #154 (payload enums get a GC
# header and an emit_val_to_string arm): 295 passed, 8 failed, 14 skipped, 1
# known-fail (pass/arg_temp_rooted, BUGS #162 — see below). The SET
# is unchanged and that is the expected result, not a null one — oracle_enum_println
# stays on this list because its FIELDLESS half is still broken, so the file's own
# rule applies in reverse here: an unchanged set does not mean nothing moved. What
# moved is inside one name. That test went from two failing lines to one:
# `[Nothing, Just(1)]` (payload variants in a list) is now correct, while
# `[Red, Blue]` (fieldless) still prints `[0, 4.77831e-299]`. A fieldless variant is
# the immediate `tag << 56` with no allocation to carry a header, so it cannot be
# identified at runtime without guessing that some doubles are enums — see the
# STATUS note at the top of test/oracle_enum_println.sf.
#
# The +4 over the 291 above is fully accounted for, and the accounting is the point:
# +1 is test/pass/enum_payload_any.sf, this fix's regression test (5 of its first 16
# assertions failed before the change; all 23 pass after, the extra 7 being the GC
# stress and recursive-format cases added while fixing the rooting bugs the first
# 16 did not reach). +2 are test/pass/explicit_any_annotation.sf
# and test/pass/list_lit_elem_type.sf, which arrived with #147 and #117 — both
# merged in after the 291 measurement, neither related to this change. The last +1
# is not a new file at all: oracle_println_class_not_bits was the sole entry in
# run_tests.sh's KNOWN_FAIL table, and #115's fix emptied that table, so it moved
# from `known-fail` to `passed`.
#
# The KNOWN_FAIL table is non-empty again, with one entry: pass/arg_temp_rooted 156.
# That test is new in this commit and fails BY DESIGN — it asserts the invariant
# #162's fix must satisfy (an object constructed as argument 0 survives argument 1
# allocating; currently 49-51 of 200 reads are corrupt). It reports as `xfail`, so
# it does not fail the suite, and when #162 is fixed it reports `xpass`, which DOES
# fail the suite until the entry is dropped. That is the intended ratchet, not an
# exemption: parking a genuinely broken test here would be the misuse the table's
# own comment warns against.
#
# Checkable rather than asserted: eligible files in test/{,pass/,fail/} went 314 →
# 318. 314 = 291+8+14+1, and 318 = 295+8+14+1 with the last term being
# pass/arg_temp_rooted. An earlier draft of this note guessed the delta was "two
# that arrived with the same run's rebuild" and named a test file that did not exist
# yet; a later one wrote ", 0 known-fail" and rested an argument on the table being
# empty, which stopped being true in the same commit. Both were wrong, which is a
# better argument for this file's compare-sets-not-totals rule than any of the green
# runs above: the set diff was empty and correct the whole time the prose about the
# totals was being invented and re-invented.
#
# Confirmed 2026-08-04 after merging the #154 payload half into main (#157, #158,
# #159, #160, the located-diagnostics and ret_type/check_assign work): 306 passed, 8
# failed, 14 skipped, 1 known-fail. The SET is unchanged and is identical to main's
# own copy of this file in both `comm` directions — the two lines of work compose
# with no name entering or leaving. Bootstrap green through STAGE 2 gen4 with 0
# unresolved inference fallbacks after the merge, which is the check that matters
# here: both sides edited src/compiler/codegen/{stmts,output}_body.sf, so a textual
# merge succeeding proves nothing about whether the merged codegen is coherent.
#
# The +11 over the 295 above is entirely main's side (its own new tests for #157 /
# #158 / #160 / assign-type / return-type / located diagnostics, all of which were
# already passing there), and eligible files went 318 → 329. The one known-fail is
# still pass/arg_temp_rooted, renumbered from #156 to #162: main had already taken
# #156 for the `var x = nil` fix and the sfx worktree had claimed #161, so the
# pre-merge next-free grep moved it twice. Worth noting against this file's rule
# about totals — 295 → 306 is a large jump with an entirely empty set diff.
#
# Confirmed 2026-08-04 after a second merge of main, picking up #163 (three
# malformed-`import` defects that failed at link time rather than parse time): 309
# passed, 8 failed, 14 skipped, 1 known-fail. SET unchanged, comm-diffed empty both
# directions against main's own copy. The +3 is #163's three tests
# (fail/import_missing_from, fail/import_unquoted_path, pass/import_forms_ok), all
# already passing on main; eligible files 329 → 332. Bootstrap green through STAGE 2
# gen4, 0 unresolved inference fallbacks.
#
# This second merge is the one worth reading twice, because #163 tightened the
# *import* path while this line's change is in codegen — and imports are resolved by
# main.sf's text scanners rather than the parser, so the two touch genuinely separate
# machinery and the empty set diff is the expected result rather than a lucky one.
# The reason to record it anyway: the first merge attempt into the shared checkout
# was aborted by git because a teammate's bootstrap was mid-write on
# build/stage3/codegen.ll. Waiting for it, then re-merging, is what turned #163 from
# something this branch would have silently reverted into something it composes with.
#
# Confirmed 2026-08-05 after FIXING #162 (argument temps GC-rooted by value across
# call setup): 310 passed, 8 failed, 14 skipped, 0 known-fail. SET unchanged,
# comm-diffed empty both directions against the 8 names at the bottom of this file.
#
# The arithmetic, checkable: 309 -> 310 passed and 1 -> 0 known-fail are the SAME
# test moving, not two changes. pass/arg_temp_rooted was the sole KNOWN_FAIL entry;
# #162's fix made it pass, the suite reported `xpass` and went RED, and the entry was
# dropped in this commit — which is precisely the ratchet the paragraph 60 lines up
# described as intended. The 9-failure run in between is real and is in the log: an
# xpass is counted as a failure on purpose. Eligible files are unchanged at 332,
# because no test file was added — the test already existed, it just started passing.
#
# Read this alongside the note above about totals. Every previous entry here argued
# that a total moving proves nothing; this one is the inverse case worth recording,
# where a total moving by exactly 1 in each of two columns is the only evidence in
# the suite output that anything happened at all. The set diff is empty and STAYS
# empty across a real fix, because the fixed test was never in the failure set — it
# was in the known-fail table, which the set diff does not read.
#
# What the suite does NOT cover, and it is the reason the fix was measured with an
# ad-hoc probe instead: the bootstrap links a base whose GC entry points are no-ops
# and never collects, so the compiler compiling itself cannot exercise a rooting bug
# at all. Coverage came from /tmp probes over five call forms (direct, instance
# method, constructor, indirect closure, list literal), which is how it was found
# that instrumenting the direct-call loop alone still left 4 of the 5 broken. Neither
# a green bootstrap nor a green suite would have shown that.
#
# pass/arg_temp_rooted grew from 2 assertions to 6 in the same commit, one per call
# form. Compiled by the PRE-fix compiler the 6-assertion version segfaults rather
# than counting mismatches (Pair holds a freed Box and `p.a.v` dereferences it),
# which is a sharper negative control than the 49/200 the 2-assertion version gave.

# Updated 2026-08-05 on the twice-merged tree (this line's #167/#168 plus origin's
# #160 closure, #164, and the VarDecl.type_ann -> AST.Type migration): 315 passed,
# 8 failed, 14 skipped, 1 known-fail. Bootstrap green through STAGE 2 (0 unresolved
# inference fallbacks; gen3 compiles itself, gen4 links and compiles).
#
# ONE NAME REMOVED, pass/check_module_imports — and it is removed for the reason
# this file's header calls out as suspect. It is NOT a fix. BUGS #162 is still
# wide open, and three independent measurements at this exact HEAD say so:
#
#   - test/pass/arg_temp_rooted.sf, #162's own regression test (origin added it as
#     a KNOWN_FAIL), still XFAILs: 1/2 assertions, expected 10, actual 49.
#   - The standalone repro under GC.set_threshold(1) — take(Box(7), alloc()) over
#     200 rounds — now reports mismatches=200. Not 51 as it did two merges ago:
#     ALL 200.
#   - Nothing in either merge touched SSA-temp rooting. origin's codegen commits
#     were the type_ann migration and #154's payload half.
#
# So the name left this list because allocation timing shifted underneath it, not
# because the defect it exposes was repaired. It is deterministic in both
# directions, which is the trap: 12/12 passing runs at this HEAD, and it was
# reliably segfaulting before. A test that flips from always-red to always-green
# with no fix in between is indistinguishable from a repair if you only read the
# totals — and the totals here look great (+4 passed, -1 failed).
#
# What replaces it as #162's acceptance test is pass/arg_temp_rooted, which is
# strictly better: it is the bug's own repro rather than a bystander, and it is
# registered as a KNOWN_FAIL so an actual fix reports as an `xpass` failure and
# forces the entry to be dropped deliberately. That is the mechanism that was
# missing when check_module_imports was carrying the signal.
#
# The count arithmetic, since a merge is where it stops closing: origin's 309
# passed / 8 failed, PLUS this line's four new tests (pass/gc_constructor_rule,
# pass/duplicate_decl_ok, fail/duplicate_class, fail/duplicate_enum_class), PLUS
# pass/check_module_imports returning to the passing column, MINUS
# pass/arg_temp_rooted leaving `passed` for the known-fail bucket. 309 + 4 + 1 + 1
# = 315 with the xfail counted separately, and 8 failed unchanged by name.

# Confirmed 2026-08-05 after merging origin's gen2 PROMOTION (VarDecl.type_ann as an
# AST.Type node): 315 passed, 8 failed, 14 skipped, 1 known-fail. The SET below is
# UNCHANGED, comm-diffed both directions, empty each way.
#
# The load-bearing detail is not the set, it is the hash: md5 of build/saffronc is
# 0044ed810447dee0976339d13dfa963c, BYTE-IDENTICAL to the gen3 built from the
# previous gen2 before the promotion. A promotion replaces the root of trust for the
# whole chain, so it is the one merge that cannot be waved through on "only a binary
# changed" — and the way to check it is that the new root reproduces the same gen3,
# not that the suite is still green. Same source, same output, different compiler
# compiling it.
#
# One visible difference in the log, and it is the expected one: STAGE 1 no longer
# prints "gen2 outdated, bootstrapping via checked-in .ll artifacts...". That line
# had been appearing on every bootstrap in this line's history, because the old gen2
# could not compile the current source directly and fell back to the checked-in .ll
# files. The promoted gen2 compiles it. Bootstrap dropped from ~6 minutes to ~2.
#
# Also recorded because it cost nothing and could have cost a false RED: this
# bootstrap sat on bootstrap.sh's LOCK for several minutes waiting for
# /tmp/saffron_154's run to finish (pid 80107, identified via `lsof -p ... | grep
# cwd`). The lock did its job. A bootstrap that appears hung with a one-line log is
# usually this, not a failure -- check for the [LOCK] line before diagnosing.

# Confirmed 2026-08-05 after rebasing the compiler-rewrite I3 line onto this tree
# (the infer_binary node migration, plus BUGS #169 — a generic return type losing
# its type arguments): 317 passed, 8 failed, 14 skipped, 1 known-fail. The SET
# below is UNCHANGED, comm-diffed both directions after stripping comments per the
# recipe above, empty each way. The +1 over 316 is this line's own
# test/pass/generic_return_infers.sf, confirmed PASS by name in the log.
#
# Why this run mattered rather than being a formality: #169's fix changes what
# `TypeEnv.get_func_ret` RETURNS — every function and method return type in the
# language now arrives at its six consumers spelled with its type arguments
# (`List<String>`) instead of collapsed to a bare base (`List`). So the risk was
# never that the fix fails to fire; it was that some consumer downstream was
# written against the collapsed spelling and silently changes answer. Two of the
# six compare the result against a string, and one feeds it to `is_type_param`,
# which is precisely where the richer spelling could be misread — a function type
# spells as "Fun(Int):Int" and would have passed every test that predicate makes.
# A name entering this list is how that would have shown up. None did, and the
# same widened predicate ran over the compiler's own source in the stage 2 gen4
# fixed point.
#
# The interesting negative result: TWO of the tests this line added earlier are
# still passing here (pass/call_arg_class_legal, and pass/any_global_widens from
# further back), and pass/arg_temp_rooted still XFAILs with "expected 10, actual
# 49" — so #162 is untouched by any of this, as expected, and the merge did not
# quietly move the allocation timing that the note two entries up warns about.
#
# One process note worth more than the result, because it is the failure this
# file's rule does not cover: the bootstrap and suite reported here are the SECOND
# pair. The first pair was green too — and measured against a parent 11 commits
# behind. Those numbers were already written into a commit message before the
# push was rejected. A green measurement on the wrong parent is not a weaker
# result than no measurement; it is worse, because it reads exactly like the right
# one. Fetch before you measure, not just before you push.

#
# Confirmed 2026-08-05 after MERGING origin/main (ff40464) into the #162 fix line:
# 318 passed, 8 failed, 14 skipped, 0 known-fail. SET unchanged, comm-diffed empty
# both directions against the 8 names below. Bootstrap green through STAGE 2 (0
# unresolved inference fallbacks; gen3 compiles itself, gen4 links and compiles) on
# origin's PROMOTED gen2 -- which is the check that matters, since the #162 fix
# touched codegen's argument loops and this merge brought the VarDecl.type_ann and
# get_func_ret AST.Type migrations that rewrote adjacent code. The +8 over 310 is
# entirely origin's own new tests (its #160/#164/#167/#168/#169 suite), all already
# passing on main; eligible files 332 -> 340.
#
# SUPERSEDES the "#162 still XFAILs" lines in the two notes just above (the twice-
# merged and rebased runs): those were written on origin's line BEFORE this fix
# landed and describe a KNOWN_FAIL table that no longer has an entry. #162 is FIXED
# and lives under Resolved; pass/arg_temp_rooted is an ordinary passing test with 6
# assertions. The stale sentences are left in place rather than edited because they
# were true when written and this file records history -- this note is the correction,
# in the position chronology puts it. The ordering hazard is real and worth naming:
# my #162 note sits ABOVE origin's later notes because my branch forked before they
# were written, so read the dates, not the position.
#
# Updated 2026-08-06: the reflect-construct codegen fix removed `json` and
# `pass/deep_deserialize` from this set — both routed through
# __reflect_construct_from_map, which (1) compared the `%klass` closure box against
# a raw ctor pointer and always missed, falling to `ret 0` → null deref segfault,
# and (2) built map lookup keys with an untagged ptrtoint that never matched the
# NaN-box-tagged stored keys. The fix calls the box as a closure to construct the
# instance, then dispatches field-fill by the instance's GC class tag. 325 passed,
# 6 failed, 14 skipped; no regressions. The 6 below are a strict subset of the
# previous 8. The remaining names are all unimplemented language features
# (varargs, function overloading, the pipe operator / atoms in `expressions`,
# docstring reflection) or the #154 fieldless-enum representation problem
# (oracle_enum_println) and the async scheduler auto-link question (async) — none
# is a plain codegen regression.
#
# Updated 2026-08-06: pass/expressions.sf DELETED, not fixed. It exercised the pipe
# operator (`|>`), Elixir-style atoms (`:atom`), and C-style `;` statement
# terminators — none of which the language defines. The pipe operator and atoms
# were explicitly dropped as language features, so the test tested syntax that will
# never exist. Removed from the tree and from this list; 5 names remain. (This line
# and the #2/#175 line both dropped the name independently — same conclusion.)

async
docstrings
oracle_enum_println
pass/overloading
pass/varargs
