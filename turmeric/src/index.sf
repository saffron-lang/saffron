// Turmeric — Reactive Web Framework for Saffron
// Barrel file: re-exports all public API via named imports

// =============================================================================
// Signals — reactive primitives
// =============================================================================

import { signal, computed, effect, batch, untrack, Signal, Computed, EffectHandle, Subscription } from "./signal.sf"

// =============================================================================
// Router — client-side SPA routing
// =============================================================================

import { hash, history, memory, Link, Router, RouteParams, RouteDef, Mode, split_path, parse_query, match_pattern } from "./router.sf"

// =============================================================================
// Events — Event class lives in prelude (auto-imported)
// =============================================================================

// =============================================================================
// Style — CSS utilities
// =============================================================================

import { styles, cx, scoped_styles, ScopedStyles, css_vars, keyframes, Keyframes, media_query, MediaQuery } from "./style.sf"

// =============================================================================
// Context — dependency injection
// =============================================================================

import { create_context, Context, provide, provide_signal, use_context, use_context_signal, scoped, ContextScope, ContextEntry, ThemeConfig, ThemeContext, AuthState, AuthContext, LocaleConfig, LocaleContext, RouterContext } from "./context.sf"
