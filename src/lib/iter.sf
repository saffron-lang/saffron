// Iterator utilities — works with any type that has .iter(), .next(), .next?()
// Lists and Maps both provide native .iter() methods.

fun map(iterable, func) {
    var result = []
    var iter = iterable.iter()
    while (iter.next?()) {
        result.push(func(iter.next()))
    }
    return result
}

fun filter(iterable, func) {
    var result = []
    var iter = iterable.iter()
    while (iter.next?()) {
        var item = iter.next()
        if (func(item)) {
            result.push(item)
        }
    }
    return result
}

fun reduce(iterable, func, initial) {
    var acc = initial
    var iter = iterable.iter()
    while (iter.next?()) {
        acc = func(acc, iter.next())
    }
    return acc
}

fun sum(iterable) {
    return reduce(iterable, fun (a, b) => a + b, 0)
}

fun any(iterable, func) {
    var iter = iterable.iter()
    while (iter.next?()) {
        if (func(iter.next())) { return true }
    }
    return false
}

fun all(iterable, func) {
    var iter = iterable.iter()
    while (iter.next?()) {
        if (!func(iter.next())) { return false }
    }
    return true
}

fun each(iterable, func) {
    var iter = iterable.iter()
    while (iter.next?()) {
        func(iter.next())
    }
}

fun count(iterable, func) {
    var n = 0
    var iter = iterable.iter()
    while (iter.next?()) {
        if (func(iter.next())) { n = n + 1 }
    }
    return n
}

fun find(iterable, func) {
    var iter = iterable.iter()
    while (iter.next?()) {
        var item = iter.next()
        if (func(item)) { return item }
    }
    return nil
}

fun flat_map(iterable, func) {
    var result = []
    var iter = iterable.iter()
    while (iter.next?()) {
        var items = func(iter.next())
        var inner = items.iter()
        while (inner.next?()) {
            result.push(inner.next())
        }
    }
    return result
}

fun zip(a, b) {
    var result = []
    var iter_a = a.iter()
    var iter_b = b.iter()
    while (iter_a.next?() and iter_b.next?()) {
        result.push([iter_a.next(), iter_b.next()])
    }
    return result
}

fun enumerate(iterable) {
    var result = []
    var iter = iterable.iter()
    var i = 0
    while (iter.next?()) {
        result.push([i, iter.next()])
        i = i + 1
    }
    return result
}

fun take(iterable, n: Number) {
    var result = []
    var iter = iterable.iter()
    var i = 0
    while (i < n and iter.next?()) {
        result.push(iter.next())
        i = i + 1
    }
    return result
}

fun skip(iterable, n: Number) {
    var result = []
    var iter = iterable.iter()
    var i = 0
    while (iter.next?()) {
        var item = iter.next()
        if (i >= n) { result.push(item) }
        i = i + 1
    }
    return result
}
